// -*- mode: jsonc -*-
{
  "layer": "top",
  "position": "top",
  "height": 30,
  "spacing": 8,

  "modules-left": [
    "hyprland/workspaces"
  ],
  "modules-center": [
    "hyprland/window"
  ],
  "modules-right": [
    "backlight",
    "pulseaudio",
    "cpu",
    "memory",
    "temperature",
    "battery",
//    "custom/net",
    "clock",
    "tray"
  ],

  // ── Hyprland ────────────────────────────────────────────────────────────────
  "hyprland/workspaces": {
    "disable-scroll": true,
    "all-outputs": true,
    "warp-on-scroll": false,
    "sort-by-number": true,
    "format": "{name}",
    "on-click": "activate",
    "format-icons": {
      "urgent": "",
      "focused": "",
      "default": ""
    }
  },

  "hyprland/window": {
    "max-length": 60,
    "tooltip": false,
    "separate-outputs": false
  },

  // ── System modules (right side) ────────────────────────────────────────────
  "backlight": {
    "format": "{percent}% ",
    "interval": 2
  },

  // Speaker + Mic in the same module
  "pulseaudio": {
    "format": "{volume}% {icon}   {format_source}",
    "format-bluetooth": "{volume}%  {icon}   {format_source}",
    "format-muted": "   {format_source}",
    "format-source": "{volume}% ",
    "format-source-muted": "",
    "format-icons": {
      "headphone": "",
      "hands-free": "",
      "headset": "",
      "phone": "",
      "portable": "",
      "car": "",
      "default": ["", "", ""]
    },
    "on-click": "pavucontrol",
    "on-click-right": "pavucontrol -t 4",
    "scroll-step": 2
  },

  "cpu": {
    "format": "{usage}% ",
    "interval": 2,
    "tooltip": false
  },

  "temperature": {
    "format": "{temperatureC}°C ",
    "critical-threshold": 85,
    "interval": 3
  },

  "memory": {
    "format": "{percentage}% ",
    "interval": 5,
    "tooltip": false
  },

  "battery": {
    "states": { "warning": 30, "critical": 15 },
    "format": "{capacity}% {icon}",
    "format-charging": "{capacity}% ",
    "format-plugged": "{capacity}% ",
    "format-alt": "{time} {icon}",
    "format-icons": ["","","","",""],
    "interval": 10
  },

  // Active interfaces + IPv4s (from custom script below)
  "custom/net": {
    "format": "{}",
    "exec": "$HOME/.config/waybar/ifaces.sh",
    "interval": 5,
    "tooltip": false
  },

  // Click to show calendar (uses gsimplecal)
  "clock": {
    "interval": 1,
    "format": "{:%Y-%m-%d %I:%M %p}",
    "tooltip-format": "<tt><small>{calendar}</small></tt>",
//    "on-click": "gsimplecal"
  },

  "tray": {
    "icon-size": 18,
    "spacing": 6
  }
}
